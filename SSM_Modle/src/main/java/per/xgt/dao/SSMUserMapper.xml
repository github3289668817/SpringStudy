<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.xgt.dao.SSMUserMapper">

    <select id="findOneById" resultType="per.xgt.pojo.SSMUser">
        select id,username "userName",password "passWord" from test_user where id = #{id}
    </select>

</mapper>