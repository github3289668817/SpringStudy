<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.xgt.mapper.OrderMapper">

    <resultMap id="orderMap1" type="order" >
        <!--手动指定字段与实体属性的映射关系-->
        <!--
            column 数据表的字段名称
            property 实体的属性名称
        -->
        <id column="oid" property="id" ></id>
        <result column="ordertime" property="ordertime"></result>
        <result column="total" property="total"></result>
        <result column="uid" property="user.id"></result>
        <result column="username" property="user.username"></result>
        <result column="password" property="user.password"></result>
        <result column="birthday" property="user.birthday"></result>
    </resultMap>

    <resultMap id="orderMap2" type="order" >
        <!--手动指定字段与实体属性的映射关系-->
        <!--
            column 数据表的字段名称
            property 实体的属性名称
        -->
        <id column="oid" property="id" ></id>
        <result column="ordertime" property="ordertime"></result>
        <result column="total" property="total"></result>
        <!--
            property 当前实体中的属性名称order中的user
            javaType 当前实体中的属性的类型
        -->
        <association property="user" javaType="user">
            <id column="uid" property="id" ></id>
            <result column="username" property="username"></result>
            <result column="password" property="password"></result>
            <result column="birthday" property="birthday"></result>
        </association>
    </resultMap>

    <select id="findAll" resultMap="orderMap2">
        SELECT * FROM test_order o,test_user u WHERE o.uid = u.id;
    </select>

</mapper>